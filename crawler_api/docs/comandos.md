# Comandos úuteis pro projeto

```bash
# Instanciando o Pipenv
python -m pipenv install

# Instanciando o playwright
playwright install

# Executar aplicação servidor
uvicorn app:app --reload
```

## Requisitos

1. Python >= `3.8`
2. 2 vCPU
3. 2GB Ram